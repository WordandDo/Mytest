# ============================================================
# 超时配置 (Timeout Configuration)
# ============================================================

# 第1层：OpenAI API调用超时（秒）
# Layer 1: OpenAI API call timeout (seconds)
# 默认值 Default: 30
OPENAI_TIMEOUT=30

# API重试次数
# API retry attempts
# 默认值 Default: 2
OPENAI_MAX_RETRIES=2

# 第2层：任务执行超时（秒）
# Layer 2: Task execution timeout (seconds)
# 默认值 Default: 600 (10分钟/minutes)
TASK_EXECUTION_TIMEOUT=600

# 第3层：资源占用超时（秒）
# Layer 3: Resource occupation timeout (seconds)
# 默认值 Default: 900 (15分钟/minutes)
RESOURCE_MAX_OCCUPATION_TIME=900
